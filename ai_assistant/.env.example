# Optional: use OpenAI embeddings instead of local sentence-transformers
# OPENAI_API_KEY=sk-...
# USE_OPENAI_EMBEDDINGS=1
# If you want query_bot.py to call OpenAI to generate final reply set:
# USE_OPENAI_MODEL=1
# OPENAI_MODEL=gpt-4o-mini or gpt-4o
